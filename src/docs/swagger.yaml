openapi: 3.0.0

info:
  title: ITicket API
  version: 1.0.0

servers:
  - url: http://localhost:3000

paths: {}

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas: {}